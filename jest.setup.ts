import { setUpTests } from "react-native-reanimated";
// import { FetchMocker, MockServer } from "mentoss";

setUpTests();

// Uncommenting this will cause a failure
// const server = new MockServer("http://localhost");
// const mocker = new FetchMocker({ servers: [server] });

// beforeAll(() => {
//   mocker.mockGlobal();
// });

// afterEach(() => {
//   mocker.clearAll();
// });

// afterAll(() => {
//   mocker.unmockGlobal();
// });
